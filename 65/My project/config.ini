# 開房間右側面板預設設定 — 放在執行檔同一層，純文字可手改。
# 改完存檔，下次開遊戲生效。
[Room]
# 速度可選清單（逗號分隔，要加/減檔位直接改）
speedSteps=1.0,1.5,2.0,2.5,3.0,4.0,5.0,6.0,8.0
# 預設速度（會對齊到上面最接近的檔位）
defaultSpeed=2.5
# 預設 note 種類(hit-effect)：-1=隨機，>=0=指定第幾種
defaultNoteType=6
# 預設組隊：0=A 1=B 2=C 3=自由
defaultTeam=3
# 預設掉落方式：0=向上 1=向下
defaultDropDirection=0
# 預設模式：0=自由模式 1=普通模式
defaultGameMode=0
