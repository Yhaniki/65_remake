# OS
.DS_Store
Thumbs.db
Desktop.ini

# Local machine-specific data-root override (SdoDataRoot.ConfiguredRoot) — points the game at a prepared DATA tree.
# 這裡(repo 根)的只有 **editor** 會讀；打包版只讀 dance.exe 同層的那一份。
data_root.txt
# 本機用 junction 組出來的 DATA 樹 + 其掛載清單 (tools/link_data_root.ps1 重建；含絕對路徑，不入庫)
/DATA/
/DATA_MANIFEST.csv
# 載入追蹤輸出 (SDO_TRACE_LOADS) — 用來抽「乾淨資料包」
/used_files*.txt

# IDE / Editor
.vs/
.vscode/
.idea/
*.suo
*.user
*.userprefs
*.sln.docstates
*.csproj.user

# Cursor（本地設定）
.cursor/

# .NET build output
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
*.dll
*.exe
*.pdb
# 但 Unity 原生外掛 DLL 是專案素材，要追蹤（例外於上面的 *.dll）
!65/My project/Assets/Plugins/**/*.dll
*.cache
project.lock.json
project.fragment.lock.json
artifacts/
*.nupkg

# Unity 自動產生的方案/專案檔（重開 Unity 會重建）
*.sln
*.csproj
*.unityproj
*.csproj.AssemblyInfo.cs

# 例外：server/ 與 tools/ 底下的專案檔是**手寫的**，必須進版控。
# 上面那兩條 *.sln / *.csproj 是為了排除 Unity 每次重開就重建的那批（65/My project 底下），
# 但它們是無路徑的 pattern，會一併吃掉我們自己寫的 —— server 的 csproj 裡有共用原始碼的
# <Compile Include> 清單與 LangVersion 鎖定，漏掉的話別人 clone 下來根本編不出 server。
!server/**/*.csproj
!server/**/*.sln
!tools/**/*.csproj

# Unity（任意子目錄下的 Unity 專案）
[Ll]ibrary/
[Tt]emp/
[Bb]uild/
[Bb]uilds/
[Uu]ser[Ss]ettings/
[Mm]emoryCaptures/
[Rr]ecordings/
sysinfo.txt
InitTestScene*.unity*
*.apk
*.aab
*.unitypackage
*.app
*.pidb
*.booproj
*.svd
*.mdb
*.opendb
*.VC.db

# Python（tools/）
__pycache__/
*.py[cod]
.Python
.venv/
venv/

# 秘密檔
.env
.env.*
*.pem
credentials.json
secrets.json

# 大型素材原始檔放 assets/ 但不入庫；保留 wireframes 參考圖
assets/*
!assets/wireframes/

# Claude Code 本地設定
.claude/

# 逆向／截圖／擷取的暫存產物（放在 repo 根目錄）
/*.png
/*.jpg
/*.log
/*-results.xml
# 測試／擷取的截圖統一落點（Sdo.Game.SdoTestOutput）：test-output/<分類>/…
# 舊的 /cross400/、/finflick/ 已併進 test-output/combo/ 底下。
/test-output/
# 字距修正時的前後對照圖與量測 log（98 個檔、15 MB，重跑截圖即可重建）
/_字距對照/
/eft_log.txt
/eft_online_log*.txt
/audio_online_log.txt
/eft_scene_log.txt
# 官方 client 的房間動作擷取(hook_mot_online 每跑一次就整份重寫,重跑即可重建)
/mot_online_log.txt
/test-results.xml
/tr.xml

# 可選：本地測譜音檔過大時取消註解
# StreamingAssets/**/*.ogg
# StreamingAssets/**/*.mp3
# StreamingAssets/**/*.wav

# Build output (regenerated by BuildScript + package_build.ps1)
/Build/

# Transient run/build artifacts
/.buildpid
/.runpid
/build.log
/cap-results.xml
/test_results*.xml
/test-*.log

# Unity auto-recovery snapshots; keep the checked-in baseline scene only.
65/My project/Assets/_Recovery/0 (*).unity*

# Procmon capture logs (large binaries; keep the scripts, not the runs)
procmon_tool/runs/

# Scratch log folder (any "新增資料夾" / "新增資料夾 (2)" …)
新增資料夾*/

# Runtime debug/probe outputs at repo root (keep the scripts, not the runs)
/defaultlog.xml
/gauge_*.txt
/shop_avatar_probe_log.txt
/tools/shop_probe/shop_avatar_online_log.txt
/tools/shop_probe/Run
/tools/scn0004_identify_log.txt

# Per-machine settings writeback (not seed profile data)
/65/My project/DATA/PROFILE/settings.json
65/My project/Assets/TextMesh Pro/Examples & Extras/
65/My project/Assets/TextMesh Pro/Examples & Extras.meta

# SdoLog runtime output (editor run writes beside the project)
/65/My project/log.txt
/65/My project/log.txt.prev

# User song library (osu!/StepMania folders dropped beside the exe) — local data, never versioned
/65/My project/Songs/

# 手打 Unity batchmode 指令時,「65\My project」沒加引號 → 路徑被空白切成「65\My」+「project」,
# Unity 的 -logFile 就落在 repo 裡一個叫 65/My 的檔上(專案根那句 "Couldn't set project path to:
# …/65/My project/…/65/My" 就是這樣來的)。它曾經被誤 commit 進版控,現在移出來並擋住。
# 🔴 這一條擋的是**檔案 65/My**,不是目錄「65/My project」—— 名字不同(後者含空白),不會被誤擋。
# 正解仍然是下指令時加引號:-projectPath "H:\...\65\My project"
/65/My

# gn_note_export 的輸出（5MB 生成檔，隨時能用 dotnet run --project tools/gn_note_export 重跑）
tools/kgn_export/gn_notes.json

# 開發過程的手動截圖（個人存檔，不入庫）
/65/My project/screensave/

# Magica Cloth 2 外掛本體在私人 repo（Yhaniki/Magica-Cloth-2），不入庫；
# Unity 產生的資料夾 meta 也一併忽略。
65/My project/Assets/MagicaCloth2/
65/My project/Assets/MagicaCloth2.meta

# lilToon (MIT) —— MMD 身體的第二種著色後端。7 MB / 幾千個檔,一行指令就能重來,所以不入庫
# (重裝指令見 docs/MMD_MODELS.md「著色後端」)。沒裝也不會壞:Shader.Find 找不到就退回 Sdo/MmdModel。
65/My project/Assets/lilToon/
65/My project/Assets/lilToon.meta
# lilToon 的 Editor 自己產生的 shader 設定(裝了才有,沒裝就是垃圾)
65/My project/ProjectSettings/lilToonSetting.json
/bubble_log*.txt
/server_run.log

# 兩開實機測試(tools/shoot_room_bubble.ps1 -M5)複製出來的 client log
/m5_log*.txt
# Unity performance-test package 跑測試時自己產生的暫存,不是專案資產
/65/My project/Assets/Resources/PerformanceTestRunInfo.json
/65/My project/Assets/Resources/PerformanceTestRunInfo.json.meta
/65/My project/Assets/Resources/PerformanceTestRunSettings.json
/65/My project/Assets/Resources/PerformanceTestRunSettings.json.meta

# 兩開實機測試時 server 的歌曲暫存(tools/shoot_room_bubble.ps1 -M5 每次重跑會清掉重建)
/server_data/

# tools/verify_online.ps1 的工作目錄(憑證、token 檔、server 暫存、兩台 client 的 log)。
# 每次重跑會整個刪掉重建 —— 報告 online_verify.md 才是要留的那份。
/.verify_online/

# cloth validation harness: regenerable artifacts (keep scripts + ref baselines + report)
tools/mmd_cloth_validate/magica_*.json
tools/mmd_cloth_validate/unity_*.log
tools/mmd_cloth_validate/*.xml
tools/mmd_cloth_validate/__pycache__/
tools/mmd_cloth_validate/probe.request
tools/mmd_cloth_validate/pmx_dump.txt

# 兩開實機測連線(MMD 模型傳檔)的產物:兩台 client 的 log 與臨時 publish 出來的 server
/mmd_a.log*
/mmd_b.log*
/server/bin-test/

# native wrapper build artifacts（原始碼與產出的 .dll 進版控，中間檔不進）
tools/sd*/*.obj
tools/sd*/*.exp
tools/sd*/*.lib
tools/sd*/*.exe
tools/sd*/*.pdb
